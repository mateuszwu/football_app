<% content_for :title, t("seasons.edit") %>

<section class="section" id="admin-season-edit">
  <div class="section__header">
    <span class="section__eyebrow">Admin</span>
    <h1><%= t("seasons.edit") %></h1>
    <p>Aktualizuj zakres sezonu i ustawienia punktacji.</p>
  </div>

  <%= render "form", season: season %>
</section>
